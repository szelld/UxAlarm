## Reminder and Alarm

The goal is to make the reminder and the alarm menus look like reminder_and_alarm.png

The app should work in the same way with the following UI and structure changes.

Change the text to icons in the bottom.

### The add button

We remove the add and edit buttons from the top.
We add edit to each reminder and alarm widget.
We add the add button to the bottom right (above the menu choices).

### Alarm

Each alarm widget should contain the following:
* Time - the time to wake the user up
* Switch - the on-off switch for the alarm
* Edit - the three dots opening the alarm edit menu

### Reminder

Each reminder widget should contain the following:
* Time - the time to remind the user
* Delete icon - to delete the reminder
* Edit - the three dots opening the reminder edit menu

### The test functionalities

Remove all the test buttons from the alarms and the reminders!

We are switching from a design prototype to a working application.
Remove all the test buttons from the edit panel.